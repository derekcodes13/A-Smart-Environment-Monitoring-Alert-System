services:
  backend:
    build:
      context: ./backend
    container_name: aies_backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    container_name: aies_frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
